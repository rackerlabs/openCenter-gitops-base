namespace: sealed-secrets

resources:
  - "./namespace.yaml"
  - "./helmrelease.yaml"

configMapGenerator:
  - name: sealed-secrets-values
    files:
      - helm-values/hardened_values_2.17.3.yaml
      - helm-values/override_values.yaml
    options:
      disableNameSuffixHash: true
