---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - "../namespace.yaml"
  - "../helmrelease.yaml"
  - "../../global/enterprise/source.yaml"
patches:
  - path: patch-helmrelease-source.yaml
    target:
      group: helm.toolkit.fluxcd.io
      version: v2
      kind: HelmRelease
      name: headlamp
      namespace: headlamp
secretGenerator:
  - name: headlamp-values-base
    namespace: headlamp
    type: Opaque
    files:
      - values.yaml=../helm-values/values-0.35.0.yaml
    options:
      disableNameSuffixHash: true
  - name: headlamp-values-enterprise
    namespace: headlamp
    type: Opaque
    files:
      - hardened-enterprise.yaml=./helm-values/hardened-values-0.35.0.yaml
    options:
      disableNameSuffixHash: true
