---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kyverno-policies
  namespace: kyverno
spec:
  releaseName: kyverno-policies
  interval: 5m
  timeout: 10m
  driftDetection:
    mode: enabled
  install:
    remediation:
      retries: 3
      remediateLastFailure: true
  upgrade:
    remediation:
      retries: 0
      remediateLastFailure: false
  targetNamespace: kyverno
  chart:
    spec:
      chart: kyverno-policies
      version: 3.5.2
      sourceRef:
        kind: HelmRepository
        name: kyverno
        namespace: kyverno
  valuesFrom:
    - kind: Secret
      name: kyverno-policies-values-base
      valuesKey: hardened.yaml
    - kind: Secret
      name: kyverno-policies-values-override
      valuesKey: override.yaml
      optional: true
