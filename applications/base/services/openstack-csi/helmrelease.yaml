---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openstack-cinder-csi
  namespace: openstack-csi
spec:
  releaseName: openstack-cinder-csi
  interval: 5m
  timeout: 5m
  driftDetection:
    mode: enabled
  install:
    remediation:
      retries: 3
      remediateLastFailure: true
  upgrade:
    remediation:
      retries: 0
      remediateLastFailure: false
  targetNamespace: openstack-csi
  chart:
    spec:
      chart: openstack-cinder-csi
      version: 2.33.1
      sourceRef:
        kind: HelmRepository
        name: cpo
        namespace: openstack-csi
  valuesFrom:
    - kind: Secret
      name: openstack-csi-values-base
      valuesKey: hardened.yaml
    - kind: Secret
      name: openstack-csi-values-override
      valuesKey: override.yaml
      optional: true
