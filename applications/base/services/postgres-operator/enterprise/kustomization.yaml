---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - "../namespace.yaml"
  - "../helmrelease.yaml"
  - "../../global/enterprise/source.yaml"
patches:
  - path: patch-helmrelease-source.yaml
    target:
      group: helm.toolkit.fluxcd.io
      version: v2
      kind: HelmRelease
      name: postgres-operator
      namespace: postgres-operator
secretGenerator:
  - name: postgres-operator-values-base
    namespace: postgres-operator
    type: Opaque
    files:
      - values.yaml=../helm-values/values-1.14.0.yaml
    options:
      disableNameSuffixHash: true
  - name: postgres-operator-values-enterprise
    namespace: postgres-operator
    type: Opaque
    files:
      - hardened-enterprise.yaml=./helm-values/hardened-values-1.14.0.yaml
    options:
      disableNameSuffixHash: true
