---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - "helmrelease.yaml"
secretGenerator:
  - name: tempo-values-base
    namespace: observability
    type: Opaque
    files:
      - values.yaml=helm-values/hardened-values-1.55.0.yaml
    options:
      disableNameSuffixHash: true
