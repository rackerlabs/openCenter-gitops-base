---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - "../namespace.yaml"
  - "../helmrelease.yaml"
  - "../../global/enterprise/source.yaml"
patches:
  - path: patch-helmrelease-source.yaml
    target:
      group: helm.toolkit.fluxcd.io
      version: v2
      kind: HelmRelease
      name: strimzi-kafka-operator
      namespace: kafka-system
secretGenerator:
  - name: kafka-api-values-base
    namespace: kafka-system
    type: Opaque
    files:
      - values.yaml=../helm-values/values-0.50.0.yaml
    options:
      disableNameSuffixHash: true
  - name: kafka-api-values-enterprise
    namespace: kafka-system
    type: Opaque
    files:
      - hardened-enterprise.yaml=./helm-values/hardened-values-0.50.0.yaml
    options:
      disableNameSuffixHash: true
